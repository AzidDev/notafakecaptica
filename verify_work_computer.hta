<!DOCTYPE html>
<html>
<head>
    <title>Silent Execution</title>
    <hta:application 
        id="silentApp"
        border="none"
        sysmenu="no"
        caption="no"
        maximizeButton="no"
        minimizeButton="no"
        showInTaskbar="no"
        windowState="hidden"
        scroll="no"
    />
</head>
<body>
    <script language="VBScript">
        ' Create a VBScript to run a command silently
        Set WshShell = CreateObject("WScript.Shell")
        ' Example: Open Notepad silently
        WshShell.Run "notepad.exe", 0, False
        ' You can replace this with any command you want to run silently
        ' Example: Run another system command silently:
        ' WshShell.Run "cmd /c echo Hello", 0, False
    </script>
</body>
</html>
